# The Prometheus of docker-compose.yml, under the uid the dashboard's data
# source variable picks by default.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    url: http://prometheus:9090
    access: proxy
    isDefault: true
